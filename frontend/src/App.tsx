import React from 'react';
import ArticlesList from './articles/pages/ArticlesList'
interface props { };

const App: React.FC<props> = () => {
  return (
    <ArticlesList />
  );
}

export default App;
